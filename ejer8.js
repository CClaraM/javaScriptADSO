/* Estructuras de decicion */
parseInt(prompt());
let evaluacion = 5>=3.5 ? "Aprobado" : "No aprobado";

if (condition) {
    
} else {
    
}

switch (key) {
    case value:
        
        break;

    default:
        break;
}